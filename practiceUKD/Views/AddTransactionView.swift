import SwiftUI

struct AddTransactionView: View {
    var body: some View {
        Text("Додати дохід/витрату")
            .font(.largeTitle)
            .bold()
            .padding()
    }
}

#Preview {
    AddTransactionView()
} 